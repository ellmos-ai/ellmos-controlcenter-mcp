# Agenten-Hinweise

- Erst `README.md`, `STATE.md` und `ARCHITECTURE.md` lesen
- Discovery- und Profil-Logik nicht in `index.ts` duplizieren
- Neue Funktionen möglichst als eigene Module unter `src/` ergänzen
- Deutsche Dokumente mit echten Umlauten schreiben

